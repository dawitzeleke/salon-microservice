s

server:
  port: 5005
spring:
  application:
    name: booking-service
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/booking
    username: root
    password:Mysql123!
    driver-class-name: com.mysql.cj.jdbc.Driver
  eureka:
    instance:
      hostname: localhost
    client:
      register-with-eureka: true
      fetch-registry: true
      service-url:
        defaultZone: "http://localhost:8070/eureka/"
  
spring.jpa.show-sql: true

